import React from 'react'
// import { Link } from 'react-router'

const Color = (props) => {

  return(
    <div className="color" style={{backgroundColor: props.something}}></div>
  )
}

export default Color
